---
import { Image } from "astro:assets";

import caseImg from "../assets/case study wholeness.jpeg";
const title = "Clinic website overhaul";
const bullets = [
  "Mobile Lighthouse: 66 → 98",
  "Lead-to-call conversion: +24%",
  "Bounce rate: –37%",
];
---

<section class="mx-auto max-w-6xl px-6 py-16">
  <div class="grid items-center gap-8 md:grid-cols-2">
    <div>
      <h2 class="font-display text-3xl md:text-4xl">Case Study — {title}</h2>
      <p class="mt-4 max-w-prose font-body">
        We rebuilt the site around speed, clarity, and outcomes. The stack is Astro, image
        optimization, and clean information architecture—no plugin bloat.
      </p>
      <ul class="mt-6 space-y-2">
        {
          bullets.map((b) => (
            <li class="relative pl-5">
              <span class="absolute left-0 top-0">•</span>
              {b}
            </li>
          ))
        }
      </ul>
      <a
        href="/contact"
        class="mt-6 inline-block rounded-pill bg-juniper px-5 py-3 text-sand ring-1 ring-transparent hover:opacity-90"
      >
        Book a walkthrough
      </a>
    </div>

    <div class="relative">
      <div class="bg-twc-gradient absolute -inset-4 rounded-2xl opacity-20"></div>
      <Image
        src={caseImg}
        alt="Case study hero"
        class="relative rounded-2xl ring-1 ring-juniper/10"
      />
    </div>
  </div>
</section>
